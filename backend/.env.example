# Backend Environment Variables (Render Environment Variables)

# Optional LLM API Keys (Users can also enter their key in the UI)
GEMINI_API_KEY=
OPENAI_API_KEY=

# Optional custom database connection string (defaults to local SQLite)
DATABASE_URL=sqlite:///enterprise_analytics.db
